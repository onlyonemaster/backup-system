# 대규모 회사 설정 (직원 200명 이상)
# 이 파일을 config.sh로 복사하세요.

COMPANY_NAME="대규모회사"
SOURCE_PHP_DIR="/home/kiam"
BACKUP_BASE_DIR="/disk/backup/edit"
CRON_INTERVAL="*/2"       # 2분마다 (즉시 감지)
RETENTION_DAYS=30         # 30일 보관 (더 긴 이력 유지)
